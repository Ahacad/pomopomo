import React, { useState } from "react";

export default function Clock() {
  return (
    <div>
      <button>start</button>
      <button>pause</button>
      <button>jump</button>
    </div>
  );
}
